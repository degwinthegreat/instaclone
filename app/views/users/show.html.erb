<h1><%= @user.name %>のページ</h1>

<p>メールアドレス:<%= @user.email %></p>

<% @favorites.each do |favorite| %>
<table>
<tr>
  <td><%= favorite.content %></td>
  <td><%= link_to 'お気に入り解除する', favorite_path(favorite), method: :delete,class: 'btn btn-danger' %></td>
</tr>
</table>
<% end %>
